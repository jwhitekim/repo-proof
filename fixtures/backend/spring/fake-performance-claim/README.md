# Catalog cache

Redis caching을 적용하여 상품 조회 성능을 80% 개선했다.

The cache implementation is present. This fixture intentionally contains no benchmark, before/after latency result, load test, representative dataset, or cache hit-ratio measurement.
